{
  "name": "exagoal-root",
  "version": "1.0.0",
  "private": true,
  "description": "ExaGoal Root Workspace Scripts",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix frontend run start",
    "lint": "npm --prefix frontend run lint"
  }
}
